package com.nebulabs.friendflix;

import android.util.Log;

import com.google.firebase.database.DataSnapshot;
import com.google.firebase.database.DatabaseError;
import com.google.firebase.database.DatabaseReference;
import com.google.firebase.database.FirebaseDatabase;
import com.google.firebase.database.ValueEventListener;

public class getFirebaseImdbId {
    //Currently under construction because i have no clue how
    //to get the data from the json file from firebase
    //and insert it into the code. This is supposed to get a random
    //id from the firebase and send it back to the MoviesData.java class
    }
